spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:library
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: data.sql
logging:
  level:
    root: info
